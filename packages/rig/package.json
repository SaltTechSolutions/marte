{
  "name": "antrenman-simulatoru",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hareket figürü motoru, kare editörü ve denetimi. Çıktısını GymEntra tüketir.",
  "scripts": {
    "editor": "node scripts/editor.mjs",
    "review": "node scripts/review-page.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "export": "npm run typecheck && npm test && node scripts/export.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
